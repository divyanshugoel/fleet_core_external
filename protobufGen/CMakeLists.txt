# #######################################################################
# Macro definations for cpp based protoc->header file generation for 
# protobuf.
# #######################################################################

set(PROTOBUF_GEN_ENABLED TRUE PARENT_SCOPE)
include(share/MacroProtocGen.cmake)